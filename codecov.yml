# One blended project number: Julia + frontend/src. Ignore committed
# IIFE/ESM bundles and tests. Flags on the upload are labels only —
# they do not split codecov/project.
ignore:
  - "assets/**"
  - "frontend/test/**"
  - "test/**"

coverage:
  status:
    project:
      default:
        target: auto
        threshold: 1%
