# Julia
/Manifest.toml
# HoloWGL has no committed Manifest by design (re-resolves to latest so the version-coupling
# guard catches drift); the e2e pre-warm + bind_notebook.jl regenerate it transiently.
/HoloWGL/Manifest.toml
*.jl.cov
*.jl.*.cov
*.jl.mem
/docs/build/

# Node / TypeScript (frontend). The built bundle assets/overlay.js IS committed.
frontend/node_modules/
frontend/dist/
*.tsbuildinfo
npm-debug.log*

# Local-only — not part of the package
/.worktrees/
/.superpowers/
/spike/
/pluto-makie-interactive-plots.md
.playwright-mcp/
*.png
