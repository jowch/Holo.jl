var documenterSearchIndex = {"docs":
[{"category":"section","location":"api/#API","page":"API","text":"julia> using Holo\n\njulia> typeof(InteractionEvent(:scatter, 0, \"a\"))\nInteractionEvent","title":"API"},{"category":"section","location":"api/#holo","page":"API","text":"holo(fig, interactables; backend = nothing, max_width = 700, selected = nothing) -> HoloWidget\nholo(fig, interactable;  …)   # single-interactable convenience\nholo(fig; …)                  # zero-config: auto-extract interactables from the plots\n\nRenders fig and overlays hit-testing for the declared interactables. Use as a Pluto @bind source; the bond value is nothing until a click, then an InteractionEvent. holo does not corrupt your figure (it saves/restores the background and runs the same finalize step Makie performs at display time).\n\nbackend — left as nothing (the default), holo picks the one backend implied by whichever of CairoMakie / WGLMakie is loaded (CairoBackend / WebGLBackend); pass one explicitly to be unambiguous or to override max_width. CairoBackend(; max_width = 700, vector = false) is the default 2D path; WebGLBackend(; px_per_unit = 2.0, max_width = 700) is the browser-GPU path (see Backends). max_width is the display width to target (Pluto's column); render resolution is derived from it (~2× the display width for CairoBackend — retina-crisp, not wasteful — never a fixed DPI). Loading neither backend raises an ArgumentError. If both are loaded, backend= wins and implicit holo defaults to Cairo.\nselected — a layer_id => indices map that pre-highlights elements on mount. See Selection.","title":"holo"},{"category":"section","location":"api/#InteractionEvent","page":"API","text":"The bond value after a click:\n\nstruct InteractionEvent\n    layer::Symbol   # the interactable's `id`\n    index::Int      # 0-based element index within the layer\n    payload::Any    # the data you attached — see note\nend\n\nPayloads round-trip as a Dict (via JSON), not the original NamedTuple. A payload (; label = \"a\") comes back as Dict(\"label\" => \"a\"), so index it as ev.payload[\"label\"]. AxisInteractable yields Dict(\"x\" => …, \"y\" => …).\n\nConstructors and plot-object overloads are tabulated under Interactables. Custom geometry: Custom interactions.","title":"InteractionEvent"},{"category":"section","location":"tooltips/#Tooltips","page":"Tooltips","text":"Hover shows a tooltip (purely client-side, no Julia round-trip). The tooltip card follows prefers-color-scheme — the same OS/browser signal official Pluto uses (there is no notebook theme toggle). Pin tooltip_* to lock colors.\n\nThe tooltip keyword is accepted by every interactable constructor. Its type governs the browser's rendering path:\n\nValue Type Browser behaviour\n(omitted / nothing) Nothing Auto name/value table built from the payload\nholo\"...\" Markup Template interpolated against the hovered element's payload\nfalse Bool Tooltip suppressed entirely\n\nWhen tooltip is not set, the browser generates a name/value table from the element's payload dict. All field names and values are HTML-escaped.\n\nPointInteractable(ax, pts; payloads = [...], tooltip = false)  # hover highlight, no card","title":"Tooltips"},{"category":"section","location":"tooltips/#holo\"...\"-templates","page":"Tooltips","text":"holo\"...\" is a string macro that produces a Markup value. $(field) is a placeholder for a browser-side payload lookup at hover time — it does not read a Julia variable.\n\ntooltip = holo\"<b>$(name)</b> — $(population:,) people\"\n\nSyntax Meaning\n$(field) Value of payload field field; HTML-escaped at hover time\n$(field:spec) Same, formatted by d3-format spec before escaping\n Literal dollar sign\n\nA fixed label with no placeholders is a template with no $() at all: holo\"<em>static label</em>\".\n\nLiteral portions of holo\"...\" are treated as raw HTML (same contract as @htl). Interpolated $(field) values and the auto-table are HTML-escaped.\n\nBecause holo\"...\" requires a string literal, a runtime-computed string must travel as a field inside the payload:\n\npayloads = [(; city, pop, label = \"$(city): $(pop) residents\") for (city, pop) in data]\ntooltip  = holo\"$(label)\"   # label is pre-rendered per element in the payload\n\nA field present in the template but absent from the payload is a build-time ArgumentError.","title":"holo\"...\" templates"},{"category":"section","location":"tooltips/#Styling","page":"Tooltips","text":"Official Pluto has no notebook light/dark toggle — Settings → Dark mode is help text. Pluto's theme CSS uses the same prefers-color-scheme query, so the card already matches stock Pluto. Pin tooltip_bg / tooltip_color to lock the card.\n\nFigure-level style overrides are keyword arguments to holo():\n\nholo(fig, interactables...;\n    tooltip_bg        = nothing,   # background  — CSS string or Makie color\n    tooltip_color     = nothing,   # text color  — CSS string or Makie color\n    tooltip_accent    = nothing,   # accent (emphasis / links)\n    tooltip_font      = nothing,   # font-family — String\n    tooltip_font_size = nothing,   # Real → appended with \"px\"\n    tooltip_radius    = nothing,   # Real → appended with \"px\"\n    tooltip_caret     = true,      # Bool — draw the caret (default: true)\n)\n\nnothing means \"use the built-in default.\" An explicitly set kwarg pins that variable, overriding dark mode. CSS custom properties (--holo-tip-*) also inherit across shadow DOM boundaries if you set them from a Pluto <style> cell.","title":"Styling"},{"category":"section","location":"custom/#Custom-interactions","page":"Custom interactions","text":"For geometry the built-ins don't cover — no JavaScript required:\n\nRegionInteractable(ax; regions, payloads, id = :region, tooltip = nothing, events = (:click, :hover)) (Tier A) — declarative mixed regions in data space, grouped into one layer per kind. Each region is one of:\n(:circle,  (cx, cy), r)            # r in data units\n(:rect,    (cx, cy), w, h)         # w, h in data units\n(:polygon, [(x, y), …])            # a ring of points\npayloads must match regions 1:1. tooltip is the same as other interactables (nothing / holo\"...\" / false); see Tooltips.\nFunctionInteractable(f; events = (:click, :hover)) (Tier B) — full control: f(ctx) -> Vector{HitLayer}. Project points with data_to_image_px(ctx, ax, point) and emit HitLayer(id, kind, geometry, payloads, axis_id(ctx, ax), events). The escape hatch for a geometry kind the others don't express.","title":"Custom interactions"},{"category":"section","location":"backends/#Backends","page":"Backends","text":"Holo has two backends. The interaction contract is the same; the cost profile is not.","title":"Backends"},{"category":"section","location":"backends/#:cairo-(the-default)","page":"Backends","text":"CairoMakie renders the figure to a PNG; Holo computes a hit-region manifest in Julia (via Makie.project) and ships it to the browser with published_to_js. A small TypeScript overlay (committed as assets/overlay.js) mounts a shadow-root layer over the image, hit-tests pointer events against the manifest, draws highlights/tooltips locally, and dispatches only deliberate clicks back through @bind. Because the image and manifest are embedded, the inspection layer keeps working in an exported, offline static HTML (only click → recompute needs a live kernel).\n\nThe default CairoBackend renders a static PNG — great for publication figures, but every update re-rasterizes the whole scene, so animation, frequent re-renders, and live 3D rotation are costly there.","title":":cairo (the default)"},{"category":"section","location":"backends/#:webgl-(WGLMakie)","page":"Backends","text":"Status: experimental / incubating. Verified end-to-end in a real Pluto notebook (render, server-free delivery, overlay, and the @bind round-trip).\n\nusing WGLMakie instead of CairoMakie switches holo to a browser-GPU backend: the figure is rendered live in a WebGL <canvas> on the client GPU, with Holo's usual overlay layered on top — same holo/@bind/ InteractionEvent contract, no code changes beyond the using line:\n\nusing Holo, WGLMakie\n\nfig = Figure()\nax = Axis3(fig[1, 1])\nscatter!(ax, x, y, z)\n\n@bind ev holo(fig)   # a live WebGL canvas + Holo's overlay; ev is an InteractionEvent on click\n\nexamples/webgl_demo.jl is a runnable gallery of the :webgl backend (CI runs it headlessly, same as demo.jl).\n\nHolo resolves the backend from which extension is loaded: a missing using line raises an ArgumentError; if both CairoMakie and WGLMakie are loaded, backend= wins and implicit holo defaults to Cairo (so a fat sysimage is not fatal).","title":":webgl (WGLMakie)"},{"category":"section","location":"backends/#How-it-works-(:webgl)","page":"Backends","text":"Julia serializes the scene with WGLMakie.serialize_scene and encodes it for the browser (a 4-rule transform: observables, GL buffers, multi-dim arrays, scalars).\nDelivery is over Pluto's published_to_js channel (scene + the WGLMakie JS bundle + a ~30-line shim), turned into blob URLs in the browser — no server, no file://, works local / remote / export.\nRender uses WGLMakie's own bundle (sourced from the installed package, so the renderer always version-matches serialize_scene) driven by the shim — no Bonito runtime.\nOverlay is Holo's existing hit-test layer, reused verbatim over the canvas; the projection is Holo's Makie.project (measured to align within ~1–2 px).","title":"How it works (:webgl)"},{"category":"section","location":"backends/#Caveats","page":"Backends","text":"Version-coupled to WGLMakie's internals (serialize_scene shape, setup_scene_init signature). Pinned to WGLMakie = \"0.13\"; treat a WGLMakie bump as a re-verification.\nThe WGLMakie bundle ships once per notebook, so each cell's own cost is just its scene.","title":"Caveats"},{"category":"section","location":"interactables/#Interactables","page":"Interactables","text":"Declare interactables explicitly (geometry in data space):\n\nPointInteractable — scatter-style points\nSegmentInteractable — lines / polylines (nearest-segment) and segment pairs\nRectInteractable — bars (list) and heatmap cells (compact grid)\nPolygonInteractable — arbitrary polygons\nAxisInteractable — the whole axis: click anywhere → data (x, y) (linear + log)\nThresholdInteractable — a draggable horizontal/vertical line; drag for a live readout, commit the data value on mouse-up\nROIInteractable — a draggable + resizable rectangle; drag the interior to move, a corner to resize; commit the data-space bounds on mouse-up\nViewInteractable — drag-to-pan (2D Axis → new limits) / drag-to-rotate (Axis3 → azimuth/elevation); commit on mouse-up; Shift+drag wins over ROI/threshold\nRegionInteractable / FunctionInteractable — custom interactions, no JavaScript required; see Custom interactions\n\nLinear, log, and categorical axes; single or multiple axes; linked selection via shared payloads through Pluto's reactive graph. Axis3 gets the same treatment on both backends: 3D scatter/lines carry point/segment overlays with {index, x, y, z} payloads, meshscatter gets depth-correct per-marker hit radii from its data-space markersize, wireframe's rendered edges are hoverable, and arrows3d shafts hit as start→end segments (processed ends, not raw pos→dir) — all projected at build time through the shared closure — static base on :cairo, live on :webgl (see Backends). Continuous pixel→data readout (AxisInteractable/ThresholdInteractable/ROIInteractable) fails loud on a 3D axis (a screen pixel is a ray, not a data point), and high-frequency live redraw is a shared cost limit on both backends. PolarAxis gets the same discrete point/segment overlays on both backends (projected through Makie.Polar in the shared closure); continuous θ/r readout is deferred. Unsupported LScene blocks fail loud at holo() time.\n\nexamples/demo.jl is a runnable gallery of every kind below plus the selection round-trip.\n\nPan, zoom, and 3D rotation use the same server-authoritative @bind re-render model on both backends: change limits (2D) or azimuth/elevation (Axis3) and rebuild — holo re-projects the overlay so hit regions never drift. Drive those params with PlutoUI sliders (no Holo API) or with ViewInteractable drag-to-pan / drag-to-rotate (commit-on-release; Shift+drag arbitrates vs box-select/ROI). examples/view_manip.jl demonstrates sliders and gestures.","title":"Interactables"},{"category":"section","location":"interactables/#Constructors","page":"Interactables","text":"Every interactable takes an Axis and geometry in data space (projected in Julia via Makie.project). All accept id — the Symbol the event reports as layer — and payloads, one entry per element (auto-generated with a 0-based index if omitted).\n\nConstructor Geometry Default payload\nPointInteractable(ax, points; id = :points, payloads, radius = 9) points :: Vector{(x, y)}; radius is the px click target (; index, x, y)\nSegmentInteractable(ax, vertices; mode = :polyline, id = :segments, payloads, tol = 6) :polyline = connected path (hit = nearest segment); :segments = disjoint pairs; tol px slack (; segment_index)\nRectInteractable(ax; rects, id = :rects, payloads) rects = [(xc, yc, w, h), …] — explicit boxes (e.g. bars) (; index)\nRectInteractable(ax; grid, id, payloads) grid = (xedges, yedges, values) — a heatmap shipped as edges, not N rects cell (i, j, value), client-side\nPolygonInteractable(ax, rings; id = :polygons, payloads) rings :: Vector{Vector{(x, y)}} — one or more filled rings (; index)\nAxisInteractable(ax; id = :axis) the whole axis: a click anywhere returns the data coordinate Dict(\"x\" => …, \"y\" => …)\nThresholdInteractable(ax; orientation = :horizontal, value, id = :threshold) a draggable line (:horizontal = constant-y, dragged vertically; :vertical = constant-x); live readout while dragging, commit on mouse-up scalar data coord (client-side, on release)\nROIInteractable(ax; bounds = (xmin, xmax, ymin, ymax), id = :roi) a draggable + resizable box; move (interior) / resize (corner); commit on mouse-up Dict(\"xmin\"=>…, \"xmax\"=>…, \"ymin\"=>…, \"ymax\"=>…) (client-side, on release)\nViewInteractable(ax; id = :view) drag-to-pan (2D) or drag-to-rotate (Axis3); commit on mouse-up; Shift+drag forces view over ROI/threshold 2D: Dict(\"xmin\"=>…, \"xmax\"=>…, \"ymin\"=>…, \"ymax\"=>…); 3D: Dict(\"azimuth\"=>…, \"elevation\"=>…)\n\nAxisInteractable, ThresholdInteractable, and ROIInteractable invert pixels→data client-side, so they support identity / log10 / log scales; any other scale fails loud at holo() time. Categorical axes are fine for AxisInteractable/ThresholdInteractable (which read a category), but ROIInteractable rejects them — its numeric bounds have no meaning on categories. ViewInteractable pan needs the same invertible continuous scales; orbit mode is Axis3-only.","title":"Constructors"},{"category":"section","location":"interactables/#From-a-plot-object-(no-hand-written-geometry)","page":"Interactables","text":"Pass the plot a plot! call returns and the geometry is pulled from it — no need to repeat coordinates you already gave Makie. These produce the same interactable the explicit constructor would, so everything above (payloads, selected, tooltips) still applies.\n\np = scatter!(ax, xs, ys; markersize = 14)\n@bind sel holo(fig, PointInteractable(ax, p))   # radius taken from markersize\n\nPlot Constructor Notes\nScatter PointInteractable(ax, p) radius defaults to markersize/2 (pixel markers); override with radius =\nLines SegmentInteractable(ax, p) :polyline (nearest-segment)\nLineSegments SegmentInteractable(ax, p) :pairs (disjoint)\nHeatmap / Image RectInteractable(ax, p) compact grid; cell (i, j, value) resolved client-side\nBarPlot RectInteractable(ax, p) reads the laid-out bars, so dodge/stack/auto-width are honored\nPoly PolygonInteractable(ax, p) one ring or many\n\nThe ax is passed because a plot has no back-reference to its axis. id/payloads take the same keywords as the explicit form (defaults: :scatter, :lines, :segments, :cells, :bars, :poly). Other plot types still need the explicit constructor.","title":"From a plot object (no hand-written geometry)"},{"category":"section","location":"interactables/#Zero-config:-holo(fig)","page":"Interactables","text":"Skip the constructors entirely — holo(fig) walks every Axis, introspects each supported plot, and overlays the lot:\n\nfig = Figure(); ax = Axis(fig[1, 1])\nscatter!(ax, xs, ys)\nheatmap!(ax, X, Y, Z)\n@bind ev holo(fig)           # both plots interactive; ev.layer tells you which was clicked\n\nLayer ids are the plot kind (:scatter, :lines, :segments, :cells, :bars, :poly), suffixed _2, _3, … when a kind repeats. Unsupported plot types are skipped with a warning.\n\nauto_interactables(fig) returns the same Vector{AbstractInteractable} holo(fig) builds, so you can grab it, tweak ids/payloads or append custom interactables, then pass it back:\n\nints = auto_interactables(fig)\npush!(ints, RegionInteractable(ax; regions = …, payloads = …))\n@bind ev holo(fig, ints)","title":"Zero-config: holo(fig)"},{"category":"section","location":"selection/#Selection","page":"Selection","text":"Pass selected to pre-highlight, and feed a bond value back to make a selection persist. The catch: feeding one widget's bond into its own selected is a Pluto reactive cycle (\"Cyclic references\") and won't run. Break it across two cells — the click source and the highlighted display — with the accumulator in between:\n\n# once: a persistent accumulator (the Ref survives later cells' re-runs)\npicks = Ref(Int[])\n\n# the click source\n@bind ev holo(fig, PointInteractable(ax, pts; id = :scatter))\n\n# accumulate clicked indices (acyclic: reads `ev` + the once-init Ref)\nselected = begin\n    ev === nothing || push!(picks[], ev.index)\n    Dict(:scatter => unique!(sort(picks[])))\nend\n\n# the display: pre-highlights `selected` on mount (its own bond is unused)\n@bind _ holo(fig2, PointInteractable(ax2, pts; id = :scatter); selected = selected)\n\nThe overlay re-derives highlights from selected on every render, so the highlighted elements survive a re-render without flicker. This is exactly the pattern in examples/demo.jl (cells under \"Selection round-trip\"), which CI runs headlessly on every change.\n\nselected is a layer_id => indices map (e.g. Dict(:scatter => [0, 2])). Indices are 0-based and match InteractionEvent.index. Supported kinds: circles / rects / polygons (selected wash) and segments / polyline (selected ring). Unsupported kinds (grid, axis, …) or out-of-range indices throw ArgumentError at build time (fail loud, like wrong-length payloads=). Keys are layer ids: for the single-layer kinds that's the interactable's id, but RegionInteractable splits into suffixed layers (:id_c circles / :id_r rects / :id_p polygons) — key on those.","title":"Selection"},{"category":"section","location":"#Holo.jl","page":"Home","text":"Light, server-free interactivity for Makie plots in Pluto — the same interactions on either backend: CairoMakie (static base) by default, or WGLMakie (live base) for animation / large data / live 3D.\n\nHolo lays a thin interactive layer over a Makie figure inside a Pluto notebook — hover for tooltips, click to select — and round-trips deliberate clicks to Julia through @bind. The default CairoMakie backend renders a publication-quality static image with a transparent JS overlay doing the hit-testing (no parallel server, no WebGL). Load WGLMakie instead and the same holo/@bind contract drives a live, browser-GPU backend for animation, large/live data, and live 3D. Exactly one backend may be loaded per Pluto session.\n\nStatus: v0.1.0. Frozen after drag-to-pan / drag-to-rotate (ViewInteractable) and overlay visual polish. Validated end-to-end in real Pluto on every supported backend — CairoMakie via examples/demo.jl, WGLMakie via examples/webgl_demo.jl (CI runs both headlessly). 0.1.x stays additive; breaking changes go to 0.2.","title":"Holo.jl"},{"category":"section","location":"#Why","page":"Home","text":" CairoMakie WGLMakie Holo\nOutput static, publication-quality live, GPU static + thin overlay\nInteractivity none rich light (hover/click)\nNeeds a live Julia process no yes only for click → recompute\nSurvives offline / static HTML export yes no yes (inspection layer)\n\nHolo fills the gap: publication-quality 2D plots with light client-side interactivity, Pluto-native, no server. By default (CairoBackend) it is not a WGLMakie replacement — static 3D only, no live camera. But holo also ships a :webgl backend: using WGLMakie instead of CairoMakie and the same API gets you live 3D, animation, and large/live data on the client GPU — see Backends.","title":"Why"},{"category":"section","location":"#Install","page":"Home","text":"julia> ] add Holo\n\nYou'll also want Pluto, plus a Makie backend: CairoMakie for the default static path, or WGLMakie for animation / large data / live 3D — a cost profile, not a feature fork. Loading both is allowed (backend= wins; implicit holo defaults to Cairo); loading neither raises an ArgumentError.","title":"Install"},{"category":"section","location":"#Quick-start","page":"Home","text":"In a Pluto notebook:\n\nusing Holo, CairoMakie\n\n# 1. your figure, as usual\nfig = Figure(); ax = Axis(fig[1, 1])\npts = [(1.0, 1.0), (2.0, 4.0), (3.0, 9.0)]\nscatter!(ax, first.(pts), last.(pts))\n\n# 2. declare what's interactable, bind the result\n@bind sel holo(fig, [PointInteractable(ax, pts; payloads = [\"a\", \"b\", \"c\"])])\n\n# 3. react to clicks — `sel` is `nothing` until a click, then an InteractionEvent\nsel === nothing ? \"click a point\" : \"you picked $(sel.payload)\"\n\nHover shows a tooltip (purely client-side, no Julia round-trip); a click sets sel and re-runs downstream cells. Clicks on empty space are a no-op. The tooltip card follows prefers-color-scheme — the same OS/browser signal official Pluto uses (there is no notebook theme toggle). Pin tooltip_* to lock colors. See Tooltips.\n\nexamples/demo.jl is a runnable gallery of every kind plus the selection round-trip.","title":"Quick start"},{"category":"section","location":"#What's-next","page":"Home","text":"Interactables — declare geometry, or pull it from a plot! return value\nCustom interactions — RegionInteractable / FunctionInteractable\nSelection — persist highlights across Pluto re-renders\nTooltips — holo\"...\" templates and tooltip_* styling\nBackends — :cairo (static PNG) vs :webgl (live canvas)\nAPI — holo, InteractionEvent, auto_interactables","title":"What's next"}]
}
