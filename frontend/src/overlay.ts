// Public entry point — kept as a stable import path for index.ts and the test suite.
// The implementation lives in mount.ts, split further into hover.ts / drag/*.ts / bond.ts /
// highlight.ts / selection.ts / state.ts.
export { mount } from "./mount"
